'use strict';
// Minimal offscreen page script used to justify persistent background for polling.
// Keeps a lightweight no-op interval to encourage runtime activity.
setInterval(() => {
  // heartbeat no-op
}, 30000);
